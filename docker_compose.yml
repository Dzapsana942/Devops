version: "3.9"

services:
  web:
    build: .
    container_name: flask-web
    ports:
      - "5000:5000"
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    restart: unless-stopped
